<!-- # New Project

This project was created from local ststem. -->



# cd folder name
  Use :- add kari ke aena pela folder ma enter thava (select karva)

# git config --global user.name "My Name" 
  Use :- Code kon upload kare 6e tenu name

# git config --global user.email "xyz@gmail.com"
  Use :- Code kon upload kare 6e tenu email

# git clone <--github repo link-->
  Use :- Repo ne Vs code ma access karva (repo hoy tenu clone aape github ma repo ma change na thay)

# git Status
  Use :- Check status ke file nu status su 6e

# git add file name ---------------------- (only one file select)
# git add . ---------------------- (All file select)
  Use :- file puch karva mare ready (like select a file for push)

# git commit -m "some message"
  Use :- file some message devo like (newfile, updatefile etc...)

# git push origin main
  Use :- file upload on github

----------------------------------------------------------------

<--- repo github ma banavane badle Vs Code mathi j banavi --->

# mkdir project name
  User :- new project using command

# git init
  User :- used to create a new git repo

# git remote add origin <--github repo link-->
  User :- local ma file repo create ne github ma upload karva

# git remite -v
  User :- verify karva ke remote sent ho gaya hai ki nuy

# git branch
  User :- to check branch

# git branch -M main
  User :- to remote branch

# git push origin main
  Use :- file upload on github

# git push -u origin main
  User :- ek var ae command set karvathi dar vakhte [git push origin main] nuy lakvu pade [git push] laksi to pan chalse

# git commit -am "some message"
  Use :- add and commit bane ek sathe [jiyare new file add na kari hoy ane ek file ma change kariyu hoy]